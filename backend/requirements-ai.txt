# innovX VisualNav - OPTIONAL learned backend.  NOT required to run.
# ---------------------------------------------------------------------------
# The prototype is fully functional on classical CV alone (requirements.txt).
# Installing this file additionally enables:
#   * DINOv2 ViT-S/14  - stronger global descriptor for candidate retrieval
#   * SuperPoint        - learned local features
#   * LightGlue         - learned feature matcher
# Every one of these is loaded lazily and falls back to the classical path if
# it is missing or fails to load (see app/models/loader.py).  No other module
# imports torch.
#
# Requires Python 3.12 (matches the main environment).  CPU is sufficient:
#   pip install -r requirements.txt
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements-ai.txt
# For CUDA, swap the index URL (e.g. .../whl/cu124).
# ---------------------------------------------------------------------------
torch>=2.2
torchvision>=0.17
# SuperPoint + LightGlue reference implementation
lightglue @ git+https://github.com/cvg/LightGlue.git
# DINOv2 is pulled at runtime through torch.hub (facebookresearch/dinov2)
