# innovX VisualNav - development / test dependencies (Python 3.12).
# Install on top of requirements.txt:
#     pip install -r requirements.txt -r requirements-dev.txt
# Then run the smoke suite from the backend/ directory:
#     pytest
pytest==9.1.1
httpx==0.28.1          # required by fastapi.testclient
